name: Go

on: [push]

jobs:
  build:
    runs-on: ubuntu:latest
    strategy:
      matrix:
        go-version: ["1.24", "1.25", "1.26"]

    steps:
      - uses: actions/checkout@v4
      # - name: Setup